# Internal Tool Libraries

Shared packages consumed by the binaries under `tools/`.

- `redact/`: redaction utilities and CLI for sanitizing Condor ClassAd logs and transfer history.

These packages are imported by sibling tool commands; they are not intended to be installed standalone.
